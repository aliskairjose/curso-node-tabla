# Notas

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la tabla de multiplicar                [número] [requerido]
  -l, --listar   Muestra la tabla en console                          [booleano] [defecto: false]
  -h, --hasta    Define el limite hasta donde se hace la tabla        [número] [defecto: 10]
```
